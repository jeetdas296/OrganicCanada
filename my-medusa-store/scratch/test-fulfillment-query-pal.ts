import { init } from "../../src/modules/fulfillment-pal/initialize" // Or wherever we can import container

// Just a quick script to test graph query for fulfillment
export default async function testFulfillmentQuery() {
  // Can't run medusa inside here easily, I'll just check the db schema.
}
